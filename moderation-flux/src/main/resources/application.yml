spring:
  main:
    web-application-type: reactive
  codec:
    max-in-memory-size: 10MB
  datasource:
    driver-class-name: oracle.jdbc.OracleDriver
    url: jdbc:oracle:thin:@(description= (retry_count=20)(retry_delay=3)(address=(protocol=tcps)(port=1522)(host=adb.ca-toronto-1.oraclecloud.com))(connect_data=(service_name=gb79ddce31ae3f6_ab69fw8uysbi8leq_medium.adb.oraclecloud.com))(security=(ssl_server_dn_match=yes)))
    username: ADMIN
    password: Study@hamilton0724

server:
  port: 8081

management:
  endpoints.web.exposure.include: health,info,metrics,prometheus
  endpoint.health.show-details: always
  endpoint.prometheus.enabled: true
  metrics.tags.application: moderation-flux

ai:
  api:
    key: "YOUR_OPENAI_API_KEY"
    url: "https://api.openai.com/v1/chat/completions"
  templates:
    # 针对自动驾驶与人行道机器人论文的深度提取模版
    autonomous-research-miner: >
      You are a Senior Research Engineer at McMaster University specialized in Autonomous Mobile Robots (AMRs). 
      Analyze the provided research text and extract technical evidence. 

      Return a JSON object with these EXACT field names (match the internal schema):
      "paperTitle": "Full title of the paper",
      "sensorModality": "e.g., Velodyne VLP-16 LiDAR + ZED Mini Stereo Camera",
      "environmentContext": "The operational setting, e.g., Urban Sidewalks, Mid-day",
      "keyMetrics": "Quantified results like LiDAR ATE or Visual Drift metrics",
      "evidenceSummary": "Concise summary of how the technology overcomes challenges",
      "sourceQuote": "One direct technical sentence from the text as proof"

      Content: {{CONTENT}}